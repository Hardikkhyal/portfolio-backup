"use client";

import GridOverlay from "@/components/layout/GridOverlay";

export default function ClientOverview() {
  return (
    <section id="client-overview" className="relative w-full h-screen z-30 overflow-hidden bg-[#19350C]">
      <GridOverlay />
      {/* Blank section as requested */}
    </section>
  );
}
